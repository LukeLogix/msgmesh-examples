{
  "name": "msgmesh-example-chat-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal web chat room starter — receive with @msgmesh/sdk's stream(), send with publish().",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "@msgmesh/sdk": "^0.2.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
