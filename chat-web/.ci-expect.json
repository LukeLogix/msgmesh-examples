{
  "_": "What CI expects of this example (read by .github/workflows/ci.yml + scripts/api-smoke.mjs). Update it together with any change to the example — this file is a declaration; CI does not infer it from the code, because inference would misread «things got deleted» as «they never existed» and silently pass.",
  "sdkMethods": [
    "publish",
    "stream",
    "streamWs"
  ],
  "buildsTo": "dist/index.html"
}
